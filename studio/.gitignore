node_modules
dist
.sanity
schema.json
.env
.env.*
!.env.example

# Video ingestion output. Transcripts for the whole catalog are megabytes and are regenerated by
# `npm run ingest:videos`, so neither the cache nor the built NDJSON is committed.
scripts/ingest/.cache
scripts/ingest/videos.ndjson
